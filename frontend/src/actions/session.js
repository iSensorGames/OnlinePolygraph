import * as userActions from "./user";

export const openConnection = query => {
  return (dispatch, getState, { socket }) => {
    const state = getState();
    const userId = sessionSelectors;
  };
};
